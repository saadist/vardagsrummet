{
  "name": "vardagsrummet",
  "version": "1.0.0",
  "main": "src/main.js",
  "scripts": {
    "start": "NODE_ENV=dev node build.mjs",
    "build": "node build.mjs",
    "clean": "rm -rf public/**/bundle.js*",
    "deploy": "firebase deploy --only hosting",
    "deploy-all": "firebase deploy"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "firebase": "^12.12.1"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}